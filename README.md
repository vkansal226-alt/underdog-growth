# Underdog Goods — Growth Loop (cloud)

Self-contained, stdlib-only growth flywheel. Runs unattended as a scheduled Claude Code routine.
No secrets committed; the routine provides ZERNIO_API_KEY via env. Nothing publishes (propose mode).
